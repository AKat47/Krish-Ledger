# Copy to .env.local and fill in your backend URL
# During local dev, leave as-is (Vite proxies /api to localhost:5000)
# After deploying backend to Render/Railway, set this to your backend URL:
VITE_API_URL=https://your-backend-name.onrender.com
